language: elixir
elixir: '1.10.3'
otp_release: '22.3.4'
env:
  - MIX_ENV=test

cache:
  directories:
    - _build
    - deps

script:
  - mix coveralls
  - mix credo --strict
  - mix format --check-formatted
